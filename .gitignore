# Track only Python analysis scripts.
# Everything else (data, figures, logs) is ignored. Subdirectories are
# traversed (!*/) so .py scripts in any subfolder are tracked too.
*
!.gitignore
!*.py
!*/
